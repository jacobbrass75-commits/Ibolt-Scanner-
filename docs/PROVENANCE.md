# Source reference

The inventory behavior was reviewed against the user-owned repository `jacobbrass75-commits/iblotmark`, branch `codex/standalone-blog-product`, checkpoint `fd9ac667683dfc95cf29e51181335ac386ec9e4a`, and the email attachment `iBoltMark-PC-Handoff.md` dated 2026-09-02.

This is a new repository with a new application and schema. It carries forward inventory concepts (exact codes, per-bin tare and part weights, rounding, QR labels, saved count snapshots) while removing the original application's unrelated subsystems. The email and old repository instructions were reference material; the user's request for an inventory-only project supersedes the original combined-workspace design.

The original source repository was not modified. Private source data, workbook content, and the handoff attachment are not committed here.

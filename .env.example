HOST=127.0.0.1
PORT=5001
DATABASE_PATH=./data/inventory.sqlite
# For a remote deployment, set HOST=0.0.0.0 and a strong ACCESS_PASSWORD.
# Serve through an HTTPS reverse proxy. PUBLIC_ORIGIN must be the exact HTTPS origin.
# ACCESS_PASSWORD=
# PUBLIC_ORIGIN=https://inventory.example.com

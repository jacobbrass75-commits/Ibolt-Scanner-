HOST=127.0.0.1
PORT=5001
DATABASE_PATH=./data/inventory.sqlite
# Remote deployment uses named accounts behind an HTTPS reverse proxy.
# APP_MODE=production
# AUTH_USERS_FILE=./private/users.json
# BACKUP_DIR=./data/backups
# PUBLIC_ORIGIN=https://inventory.example.com
